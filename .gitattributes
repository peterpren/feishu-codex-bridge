# Pin LF on shebang scripts and the bin entry so a Windows checkout with
# core.autocrlf=true can't rewrite them to CRLF (a CRLF shebang breaks
# `#!/usr/bin/env node`).
*.sh text eol=lf
*.mjs text eol=lf
bin/* text eol=lf
